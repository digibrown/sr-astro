---
import BaseLayout from './BaseLayout.astro';
const { title } = Astro.props;
---
<BaseLayout title={`${title} • SendRemit Guides`}>
  <article class="card">
    <header style="margin-bottom: 12px;">
      <h1 style="margin:0 0 6px;">{title}</h1>
      <p class="muted" style="margin:0;">A practical guide from SendRemit</p>
    </header>
    <slot />
  </article>
</BaseLayout>

